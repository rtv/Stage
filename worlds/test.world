
# Desc: 1 robot with player, laser, sonar and gps
# CVS: $Id: test.world,v 1.14 2004-09-25 02:15:00 rtv Exp $

# the size of a pixel in Stage's underlying raytrace model in meters
resolution 0.02 

interval_sim 100  # milliseconds per update step
interval_real 10 # real-time milliseconds per update step

include "pioneer.inc"  # defines Pioneer-like robots
include "map.inc" # defines 'map' object used for floorplans

map(	
  size [15.5 11.4]
  bitmap "bitmaps/autolab.png"
)


define sicklaser laser
(
  laser.range_min 0.0
  laser.range_max 8.0
  laser.fov 180.0
  laser.samples 180

  color "blue"
  size [ 0.14 0.14 ]	
)

# extend the pioneer2dx definition from pioneer.inc
#
define laserpioneer pioneer2dx
(
  sicklaser( 
    fiducialfinder( color "green" size [0.05 0.05] obstacle 0 )
  )
)


#laser( pose [3 3 0] )

laserpioneer
(
  name "myrobot1"
  pose [-1 -4 0]
  gui.nose 1 

  blobfinder(
    ### TODO - problem parsing this line?
   # blob.channels [ "red" "green" "blue" "cyan" "yellow" "magenta" ] 
  )	

  #drive "omni"
)

# a silly object to look at
position( 	
  name "sub" 
  size [2 1]
  pose [-2.0 0.0 0.0] 
  color "red"  
  bitmap "bitmaps/submarine.png"
  gui.nose 1
  
  fiducial_id 22

  laser.return 2

  velocity [0 0 10]

  drive "omni"
)
